# Generated. Regenerate with the scripts in scripts/.
models/
runs/
kb/store/
__pycache__/

# ADK writes a session DB here on every `adk web` / `adk run`.
# It is a binary, it churns, and it makes the firewall grep report a match.
.adk/
